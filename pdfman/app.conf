{
  lang => 'ja',
  iconv_ja => "nkf -w",
  groff => "env GROFF_TMAC_PATH=. groff -Tpdf -P-d -mandoc -k -t",
  groff_ja => "env GROFF_TMAC_PATH=. groff -Tpdf -P-d -mandoc -mja -k -t -p",
  #gs => "gs -sDEVICE=pdfwrite -dPrinted=false -dNOPAUSE -dQUIET -dBATCH -sOutputFile=- -",
  #force => 1,
  issues => '/vagrant/pdfman/issues.t',
}
